# RAG Service Configuration
RAG_PORT=8765
RAG_REBUILD_SECRET=changli_rebuild

# PostgreSQL for dynamic data (projects, certificates)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
